export default class Chart {
    constructor (name, unit, parameter, total, available){
        this.name = name
        this.unit = unit
        this.parameter = parameter
        this.total = total
        this.available = available
    }
}